#!/usr/bin/env bash
#
# Set login environment variables on macOS

grep "^export" "$HOME/.profile" "$HOME/.dotfiles/shell/environment" | while IFS=' =' read ignoreexport envvar ignorevalue; do
    launchctl setenv "${envvar}" "${!envvar}"
done
