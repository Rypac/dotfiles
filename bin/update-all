#!/usr/bin/env bash

: "${DOTFILES_HOME:="$HOME/.dotfiles"}"

# dotfiles
update-dotfiles

# brew packages
brew update
brew upgrade
brew bundle --file="$DOTFILES_HOME/mac/Brewfile"

# app store
mas upgrade
